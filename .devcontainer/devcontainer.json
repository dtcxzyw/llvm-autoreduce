{
    "name": "llvm-autoreduce Dev Container",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": ".."
    },
    "workspaceMount": "source=${localWorkspaceFolder},target=/llvm-autoreduce,type=bind,consistency=cached",
    "workspaceFolder": "/llvm-autoreduce",
    "mounts": [],
    "features": {
        "ghcr.io/devcontainers/features/common-utils:2": {
            "installZsh": "true",
            "configureZshAsDefaultShell": "true",
            "username": "llvm-autoreduce",
            "upgradePackages": "true"
        }
    },
    "customizations": {
    },
    "containerUser": "llvm-autoreduce",
    "remoteUser": "llvm-autoreduce",
    "runArgs": [
        "--network=host",
        "--cap-add=SYS_PTRACE",
        "--cap-add=PERFMON",
        "--security-opt",
        "seccomp=unconfined"
    ]
}
