Read `{issue_file}` for the bug report and `{reproducer_file}` for all reproducer files.

Check ALL reproducer content for malicious patterns (system/exec/fork/popen calls, file tampering, network operations, obfuscated shell commands, suspicious includes).

Write your verdict to `review.json` in this exact format:
{{
  "valid": true,
  "malicious": false,
  "reason": "short explanation"
}}
