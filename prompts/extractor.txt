Read `issue.md` for the bug report. Inspect ALL files in the working directory for reproducer code — Godbolt sources (`godbolt_1`, `godbolt_2`, ...), attachments (`attachment1`, `attachment2`, ...), and inline code blocks in the issue body. Use bash to inspect files and run LLVM tools.

CRITICAL: Do not read or write any files outside the current working directory. All operations are confined to this directory. Never access /tmp, /home, /etc, /var, or any other system path.

Files have no extensions — you MUST identify each file's actual type by reading its first 5-10 non-empty lines. LLVM IR files start with `; ModuleID`, `target triple`, `define`, `declare`, or `source_filename`. C/C++ files contain `#include`, function signatures, etc. Do NOT use the `file` command — it cannot distinguish C from C++.

Your job:
1. Identify each reproducer's actual type (LLVM IR vs C vs C++ vs assembly). If the file is LLVM IR, its first non-empty line typically contains `; ModuleID`, `target triple`, `define`, `declare`, or `source_filename`.
2. If any reproducer is C/C++ source, compile it to IR first: use `clang -x c -S -emit-llvm -Xclang -disable-O0-optnone <source> -o <output>.ll` (for C) or `clang -x c++ -S -emit-llvm -Xclang -disable-O0-optnone <source> -o <output>.ll` (for C++).
3. Attempt to reproduce the bug using the reproducer file and the pipeline mentioned in the issue. Capture the output — this validates that the reproducer is functional and provides an accurate crash signature.
4. Classify the bug type: `crash` (opt/llc/lli crash), `miscompilation` (wrong code), or `unrelated`
5. Identify the primary LLVM IR reproducer file name (MUST be a .ll file)
6. Extract a literal substring from the crash output that uniquely identifies this crash (empty string for miscompilation). Do NOT use regex — produce a plain text fragment.
7. Determine the opt pipeline from the issue

Write your findings to `extract.json` in this exact format:
{
  "bug_type": "crash",
  "reproducer_file": "filename.ll",
  "crash_pattern": "a literal text fragment from the crash output (substring, NOT a regex — e.g. failed at LICM.cpp not failed.*LICM), or empty string for miscompilation",
  "pipeline": "-passes='...'"
}
